# urdu2roman-transliterator
PyTorch-based NLP project for transliterating Urdu script into Roman Urdu. Implements custom BPE tokenizer from scratch, BiLSTM encoder + LSTM decoder seq2seq model, multiple experiments, and evaluation with BLEU/CER. Includes a public Streamlit app for interactive transliteration.
